# Скопируйте в /etc/kit20.env (или .env на сервере) и выставьте права chmod 600.
# См. README — раздел «Деплой».

ADMIN_PASSWORD=kit20
# Опционально: отдельный секрет для cookie-сессии админки
# ADMIN_SESSION_SECRET=

HOST=0.0.0.0
PORT=4321

NODE_ENV=production
